import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin | Tim & Iyla's Wedding",
  description: "Admin dashboard for Tim & Iyla's wedding website.",
  robots: {
    index: false,
    follow: false,
  },
};
